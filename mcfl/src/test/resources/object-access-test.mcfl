# Object name 'obj' exists with 3 properties
# and 'x' and 'y' property are values, and
# a callable property 'func'

$debugger "obj.x=${obj.x}"
$debugger "obj.y=${obj.y}"

$obj.x = 4012
$debugger "obj.x after update: ${obj.x}"

$debugger "obj.func().x=${obj.func().x}"

$obj.func()